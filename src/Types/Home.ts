export type TSellingPoints = {
  id: number;
  icon: JSX.Element;
  title: string;
  description: string;
};

export type TReview = {
  id: number;
  name: string;
  rating: number;
  comment: string;
};
